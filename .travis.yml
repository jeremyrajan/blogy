language: node_js
node_js:
  - "9.3.0"

before_install:
  - npm install gulp -g

script:
  - gulp --verbose

cache:
  directories:
  - bower_components
  - node_modules

notifications:
  email:
    - jeremyrajan@gmail.com
